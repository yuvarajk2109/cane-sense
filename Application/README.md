# CaneSense - Running the Application

1. Ensure all the instructions are followed as given in installs.txt.
2. Execute the application (app.py) with the command:

```bash
python app.py
```

The backend and frontend scripts will start and the application can be accessed at the given localhost port.
